{% extends 'base.html.twig' %}

{% block title %}Edit Reservation{% endblock %}

{% block body %}
    <h1>Edit Reservation</h1>
       <div class="card">
                              <div class="card-body">
                
                <h5 class="card-title" style="margin-left:300px;">Modifier Reservation</h5>
                <div class=" align-items-center justify-content-start" style="margin-left:300px"> 

    {{ include('reservation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_reservation_index') }}" class="btn btn-warning>back to list</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}

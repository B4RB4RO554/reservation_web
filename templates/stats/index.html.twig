{% extends 'base.html.twig' %}


{% block body %}
{% include 'partials/Header.html.twig' %}
   <div class="card">
                <div class="card-body">
                
                <h5 class="card-title" style="margin-left:300px;">Statistique</h5>
                <div class=" align-items-center justify-content-start" style="margin-left:300px"> 
    <div class="container">
        <h1> &nbsp &nbsp &nbsp &nbsp &nbsp &nbsp     reservation par type </h1>
        <canvas id="matchs" width="1000" height="500"></canvas>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/chart.js@2.9.4/dist/Chart.min.js" integrity="sha256-t9UJPrESBeG2ojKTIcFLPGF7nHi2vEc7f5A2KpH/UBU=" crossorigin="anonymous"></script>
    <script>
        let liv = document.querySelector("#matchs")
        let matchGraph = new Chart(liv, {
            type: "pie",
            data: {
                labels:  ['complet', 'par place'],
                datasets: [{
                    label: "Répartition des catégories",
                    data: ['{{ liv }}', {{ cl }}],
                    backgroundColor: ['#FF5733','#FFBD33']
                }]
            }
        })
    </script>
    </div>                </div>
 
   <a href="{{ path('app_reservation_index') }}"class="btn btn-warning">Return</a>
{% endblock %}